% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readcsvs.R
\name{read_csvs}
\alias{read_csvs}
\title{Read and Combine Multiple CSV Files}
\usage{
read_csvs(pattern = "*", quote = "\\"", delim = ",", skip = 0, header = TRUE)
}
\arguments{
\item{pattern}{Character string specifying file pattern to match (without .csv extension).
Supports wildcards. Default is "*" to match all CSV files.}

\item{quote}{Character used to quote fields in the CSV files. Default is "\"".}

\item{delim}{Field delimiter character. Default is ",".}

\item{skip}{Integer number of lines to skip at the beginning of each file. Default is 0.}

\item{header}{Logical indicating whether the first row contains column names. Default is TRUE.}
}
\value{
A data frame with a 'filename' column (source file without path/extension)
plus all columns from the matched CSV files.
}
\description{
Uses DuckDB to efficiently read and union multiple CSV files matching a pattern.
Automatically adds a filename column to track the source of each row.
}
\examples{
\dontrun{
# Read all CSV files
data <- read_csvs()

# Read files matching a pattern
sales <- read_csvs(pattern = "sales_*")

# Handle tab-delimited files
tsv_data <- read_csvs(pattern = "data_*", delim = "\t")
}

}
